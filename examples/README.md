# Examples

`test_query.py` connects through the proxy using these environment variables:

- `PGHOST`
- `PGPORT`
- `PGUSER`
- `PGPASSWORD`
- `PGDATABASE`

Use only against a disposable development database.
